# Public environment variables for the frontend (Vite)
# Leave empty in development (Vite proxy handles /api → localhost:5000)
# Set this to your Render backend URL when deploying to Vercel:
VITE_API_URL=
